---
spring:
  cloud:
    config:
      server:
        git:
          uri: ${vcap.services.config-server-uri.credentials.uri}     
          
security:
  user:
    username:  ${vcap.services.config-basic-auth.credentials.username}
    password:  ${vcap.services.config-basic-auth.credentials.password}

spring:
   profiles cloud

message: Morgan
